#include <stdio.h>
/**
 * main - programe prints its name followed by a new line
 * @argc: number of argument
 * @argv: array that contains arguments
 * Return: (00
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
