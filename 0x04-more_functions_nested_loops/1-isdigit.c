#include "main.h"

/**
 * _isdigit - a function that checks for a digit (o through 9)
 * @c: inter to be checked
 * Return: 1 (Success) 0 (Failure)
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
